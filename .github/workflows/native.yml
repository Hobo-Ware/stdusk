name: native (rust)

on:
  push:
    branches: [main]
    paths: ['src/**', 'Cargo.toml', 'Cargo.lock', 'build.rs', 'assets/**', 'packaging/**', '.github/workflows/native.yml']
  # No paths filter on PRs: the `check` job is a REQUIRED status check, so it must run on
  # every PR - a filtered check that skips reports nothing and blocks the merge forever.
  pull_request:
    branches: [main]

jobs:
  check:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: .
      - name: fmt
        run: cargo fmt --check
      - name: clippy
        run: cargo clippy --all-targets -- -D warnings
      - name: test
        run: cargo test --all-targets
